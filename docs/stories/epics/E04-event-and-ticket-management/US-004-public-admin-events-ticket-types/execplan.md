# Execution Plan

1. Add E04 application DTOs, commands, service interface, service implementation, and cache invalidation boundary.
2. Add public and admin event controllers with request DTO validation.
3. Add focused application service tests for public filtering, admin mutation, validation, and soft-delete behavior.
4. Add MockMvc tests for public route access and admin route authorization.
5. Run Maven validation and update `docs/TEST_MATRIX.md`.
